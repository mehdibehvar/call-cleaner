# Call Cleaner
#Getting Started

1. Clone the repo
2. Install dependencies
3.sudo mongod --config /etc/mongod.conf 
and then in another command line pnpm dev:full
4. pnpm start-server
5. pnpm dev
6. Open http://localhost:3000
