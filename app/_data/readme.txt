_data

Purpose: Sometimes used as a data access layer, specifically for fetching raw data 
(from database, CMS, or APIs).

Difference from _services:

_data is often more low-level, returning raw results.

_services may transform, combine, or enrich data for use in the UI.